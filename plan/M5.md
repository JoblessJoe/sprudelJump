# Milestone M5 — Full reset()/step()/done, matching the interface contract exactly

Spec: `plan/CORE_SPEC.md`.

Self-check:
```python
env = SprudelJumpEnv()
s = env.reset()
assert len(s) == 12
steps = 0
done = False
while not done and steps < 5000:
    s, fitness, done = env.step(random.random())
    steps += 1
print(steps, fitness)
```
Confirm it terminates (game over is reachable) and `fitness` ends up a sane,
positive-ish number.

When it passes: continue to `plan/M6.md` (optional, only if time/context remains) or
finish per `CORE_SPEC.md`'s working instructions. Don't re-read earlier milestone
files or `CORE_SPEC.md` again unless your context was reset.
