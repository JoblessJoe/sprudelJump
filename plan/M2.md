# Milestone M2 — Horizontal movement + screen wrap

Spec: `plan/CORE_SPEC.md`.

Self-check: run 100 steps with `action = 1.0`, confirm `player.x` increases then
wraps to a negative value once past `SCREEN_WIDTH`.

When it passes: continue to `plan/M3.md`. Don't re-read this file, `M1.md`, or
`CORE_SPEC.md` again unless your context was reset.
