# Milestone M1 — Gravity + one fixed platform

No scrolling, no horizontal input, no screen wrap yet. Spec: `plan/CORE_SPEC.md`.

Self-check: call `step()` 200 times with `action = 0.5`, print `player.y` every 20
steps. Should oscillate (fall, bounce, fall, bounce), never diverge.

When it passes: continue to `plan/M2.md`. Don't re-read this file or
`CORE_SPEC.md` again unless your context was reset.
