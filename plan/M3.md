# Milestone M3 — Multiple platforms + real collision

Only bounce when actually landing on a platform. Spec: `plan/CORE_SPEC.md`.

Self-check: place 3 platforms at different x/y (each with a `width`), run 300 steps
with random actions, count and print how many bounces occurred (should be several,
not zero, not every single frame).

When it passes: continue to `plan/M4.md`. Don't re-read earlier milestone files or
`CORE_SPEC.md` again unless your context was reset.
